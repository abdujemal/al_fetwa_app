import 'package:flutter/cupertino.dart';

class Answer {
  String qid, answer, answeredBy, date;
  Answer(
      {@required this.qid,
      @required this.answer,
      @required this.answeredBy,
      @required this.date});
}
